# Operational scripts

One-off development and operations scripts live here. Examples include Firestore seed data, MongoDB-to-Firestore migration utilities, index checks, and local health probes. Scripts must be idempotent where possible and must read secrets from environment variables rather than embedding credentials.
